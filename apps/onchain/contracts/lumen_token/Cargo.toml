[package]
name = "lumen_token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = "20.5.0"

[dev-dependencies]
soroban-sdk = "20.5.0"

[features]

